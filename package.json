{
  "name": "mercadopro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "e2e": "pnpm --filter server e2e:db && pnpm --filter client build && playwright test",
    "e2e:ui": "pnpm --filter server e2e:db && pnpm --filter client build && playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "concurrently": "^9.2.0",
    "wait-on": "^8.0.4"
  }
}
